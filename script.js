const byteSize = (str) => {
  // write your code here
	byteSize(str)
	if(byteSize(str)==''){
		return byteSize(0)
	}
};
return byteSize
// Do not change the code below

const str = prompt("Enter some string.");
alert(byteSize(str));